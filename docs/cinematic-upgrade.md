# Cinematic Upgrade

The `cinematic-upgrade` workflow is a non-mutating planning example for upgrading an existing project with stronger motion, visual polish, and premium interaction guidance.

Run:

```bash
orchestrator-mcp cinematic-upgrade --path ./your-project
```

This workflow is implemented as a skill pack so it can remain optional.
