# Benchmarks

The benchmark command is a lightweight routing smoke test today:

```bash
orchestrator-mcp benchmark
```

Future work:

- shared benchmark tasks
- route quality comparisons
- provider success tracking across environments
